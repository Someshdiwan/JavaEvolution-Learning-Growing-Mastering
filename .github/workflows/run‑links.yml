name: Run Links Script

on:
  workflow_dispatch:  # allows manual trigger
  push:
    branches:
      - master

jobs:
  run-script:
    runs-on: windows-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run Links.cmd
        shell: cmd
        run: |
          cd "New JDK Features\JDK Features"
          Links.cmd
